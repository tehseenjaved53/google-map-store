// script =====================================================//$(document).load(function () {});$(document).ready(function () {    window.windowWidth = $(window).width();    function collapsIng() {        var $collasingElement = $('.js-collapse');        $collasingElement.collapse().attr('data-toggle', 'collapse').attr('data-target', '.sub-menu');        $collasingElement.parents('.menu__item').hover(            function () {                $(this).find('.collapse').collapse('show');                $(this).addClass('is-hover');            }, function () {                $(this).find('.collapse').collapse('hide');                $(this).removeClass('is-hover');                $(this).attr('aria-expanded', 'false');            }        );    }    collapsIng();
    $('.js-lazy').lazy({        scrollDirection: 'vertical',        visibleOnly: false,        effect: "fadeIn",        effectTime: 500,        threshold: 0,        defaultImage: "",        enableThrottle: true,        throttle: 250,        onError: function (element) {            var imageSrc = element.data('src');            console.log('image "' + imageSrc + '" could not be loaded');        }    });    function lazyInit() {        window.windowWidth = $(window).width();        if ( window.windowWidth >= 576 ) {            $('.js-lazy-smminw').lazy({                scrollDirection: 'vertical',                visibleOnly: false,                effect: "fadeIn",                effectTime: 500,                threshold: 0,                defaultImage: "",                enableThrottle: true,                throttle: 250,                onError: function (element) {                    var imageSrc = element.data('src');                    console.log('image "' + imageSrc + '" could not be loaded');                }            });        }        if ( window.windowWidth >= 768 ) {            $('.js-lazy-mdminw').lazy({                scrollDirection: 'vertical',                visibleOnly: false,                effect: "fadeIn",                effectTime: 500,                threshold: 0,                defaultImage: "",                enableThrottle: true,                throttle: 250,                onError: function (element) {                    var imageSrc = element.data('src');                    console.log('image "' + imageSrc + '" could not be loaded');                }            });        }    }    lazyInit();    $(window).resize(function () {        lazyInit();    });
    /** modal */    (function () {        var $btn = $('.js-open-modal');        if ($btn.length) {            $.each($btn, function () {                $(this).on('click', function (e) {                    e.preventDefault();                    var                        $modalName = $(this).data('modal'),                        $modalToShow = $('#modal-' + $modalName);                    if (!$modalToShow.length) {                        console.log('Модальное окно с идентификатором #modal-' + $modalName + ' отстутвует на странице или указано не верно.');                    } else {                        $modalToShow.modal('show');                    }                });            });        }        /** close current modal and open new */        $(function () {            return $(".modal").on("show.bs.modal", function () {                var curModal;                curModal = this;                $(".modal").each(function () {                    if (this !== curModal) {                        $(this).modal("hide");                    }                });            });        });        //// uncomment if need reset validation when close modal width form        // $(".modal").on("hide.bs.modal", function () {        //        var validator = $( ".js-form" ).validate();        //         validator.resetForm();        //         // console.log("form unvalidated");        // });    })();    // (function(){    //    //     var $el = $('__________');    //    //     if ($el.length) {    //    //     }    //    // })();});